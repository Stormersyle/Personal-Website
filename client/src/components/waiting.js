import React from "react";
import "../stylesheets/waiting.css";

const Waiting = () => {
  return (
    <div>
      <h1>Loading content...</h1>;
    </div>
  );
};

export default Waiting;
